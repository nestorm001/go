package main

// import "fmt"
// import "time"

func main() {
	print("go ni da ye\n")
//	var i int
//	var j int
	i := 1
	j := 1
	i++
	print(i, "\n", j, "\n")
	
	/* t1 :=time.Now()
	count := 0
	for i := 0; i < 10000000000; i++{
		count = count + i
	}
	t2 := time.Now()
	fmt.Printf("cost: %d, count: %d\n", t2.Sub(t1), count)
	
	t3 :=time.Now()
	count1 := 0
	for i := 0; i < 10000000000; i++{
		count1 += i
	}
	t4 := time.Now()
	fmt.Printf("cost: %d, count: %d\n", t4.Sub(t3), count1) */
}